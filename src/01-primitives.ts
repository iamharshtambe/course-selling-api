// Who am I? Let's define my name as a string
const myName: string = 'Harsh';

// Age is just a number — literally!
const myAge: number = 22;

// Love life status: strictly boolean 🚫💍
const isMarried: boolean = false;

// Time to spill the tea ☕ (aka print the details)
console.log(
   `My name is ${myName}. I am ${myAge} years old and my marital status is ${isMarried}`
);
